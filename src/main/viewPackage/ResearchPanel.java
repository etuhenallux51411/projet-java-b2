package main.viewPackage;
import javax.swing.*;
public class ResearchPanel extends JPanel{
    public ResearchPanel(){
        JLabel label = new JLabel("Research Panel");
        add(label);
    }
}
